# Context
We are reviewing the implementation of US 29 for RisoTron Studio.
User Story: [US] Store and retrieve app settings (Config Manager & VSCode Settings UI).
Branch: `feat/data-layer/config-manager`

# Objective
Perform a strict code review on the implementation of the `ConfigService`, IPC bridge, and the frontend `SettingsView` (VSCode styling). 

# Input Files to Review
- `src/main/services/ConfigService.ts`
- `src/main/index.ts`
- `src/preload/index.ts`
- `src/shared/types/settings.ts`
- `src/renderer/components/SettingsView.svelte`

# Instructions
1. Review the `ConfigService` database interactions (`UPSERT`, `safeJsonStringify`). Ensure no data can be corrupted or leak.
2. Review the IPC integration (Main `app:set-setting`, `app:get-settings`, and Preload exposure). Ensure `validateSender` is used and there's no privilege escalation.
3. Review the Frontend code (`SettingsView.svelte`) for state handling and input debouncing.
4. Provide a summarized list of major blockers (if any) or approve the implementation.

# Constraints
- Be highly adversarial. Look for edge cases.
- **MANDATORY:** You MUST output your estimated token used at the very end of your response.

# Expected Output Format
Markdown review report detailing any critical bugs, structural issues, or an "Approved" statement.
